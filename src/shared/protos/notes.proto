syntax = "proto3";

package nm;

message Note {
  uint32 pitch = 1;
  float duration = 2;
  float delta = 3;
}

message Sample {
  string id = 1;
  float perplexity = 2;
  float epoch = 3;
  repeated Note notes = 4;
}
