syntax = "proto3";

package nm;

message Note {
  uint32 pitch = 1;
  float duration = 2;
  float delta = 3;
}

message Sample {
  float perplexity = 1;
  float epoch = 2;
  repeated Note notes = 3;
}
